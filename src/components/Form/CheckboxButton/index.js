import { pluginFactory } from '../../../utils/plugins';
import VCheckboxButton from './CheckboxButton.vue';

const CheckboxButtonPlugin = /* #__PURE__ */ pluginFactory({
  components: {
    VCheckboxButton
  }
});

export {
  CheckboxButtonPlugin, VCheckboxButton
};
